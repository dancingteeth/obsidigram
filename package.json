{
  "name": "obsidigram",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "plugin-archive": "./scripts/build-plugin-archive.sh"
  }
}
